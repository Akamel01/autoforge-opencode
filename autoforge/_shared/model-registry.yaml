# AutoForge model registry — capability + window metadata for model-aware delegation
# Orchestrator MUST lookup resolved model here before sizing Task prompts.
# Update via `node scripts/sync-models.mjs --check` (warns on missing `opencode models` entries).
# Windows are upstream documented values (approx tok); guidance encodes sizing heuristic.
# Do not store secrets here.

defaults:
  unknown_model:
    context_window: 64000
    max_output: 8192
    capabilities: [tools]
    cost: unknown
    strengths: []
    guidance: "Conservative: summarize inputs, cap prompt at 30k tok, require concise Return."

models:
  opencode/gpt-5-nano:
    context_window: 400000
    max_output: 128000
    capabilities: [tools, reasoning, structured-output, fast]
    cost: low
    strengths: [high-throughput, small-module execution, worker]
    guidance: "Budget 80k tok (cap 400k*0.30). Keep worker prompts checklist-style despite large window."

  opencode/gpt-5:
    context_window: 400000
    max_output: 128000
    capabilities: [tools, reasoning, vision, structured-output]
    cost: high
    strengths: [planning, critique, architecture, orchestrator]
    guidance: "Budget 80k tok (cap). Can ingest tracker-index + 2 reports verbatim; include rationale."

  opencode/gpt-5.1:
    context_window: 400000
    max_output: 128000
    capabilities: [tools, reasoning, vision]
    cost: high
    strengths: [planning, critique]
    guidance: "Budget 80k tok (cap). Prefer richer instructions."

  opencode/claude-sonnet-4-5:
    context_window: 200000
    max_output: 64000
    capabilities: [tools, reasoning, long-context, code]
    cost: high
    strengths: [deep review, validator, investigator]
    guidance: "Budget 60k tok. Deep read, cite file:line, allow 80k context."

  opencode/claude-opus-4-5:
    context_window: 200000
    max_output: 64000
    capabilities: [tools, reasoning, long-context, code, vision]
    cost: very-high
    strengths: [architecture, investigation]
    guidance: "Budget 60k tok. Full reasoning, cross-module checks."

  opencode/gemini-3-flash:
    context_window: 1000000
    max_output: 32768
    capabilities: [tools, reasoning, long-context, fast]
    cost: medium
    strengths: [large codebase scan]
    guidance: "Budget 80k tok (cap). Can ingest many files; still summarize for cost."

  opencode/gemini-3.1-pro:
    context_window: 1000000
    max_output: 65536
    capabilities: [tools, reasoning, long-context, vision]
    cost: high
    strengths: [discovery, large-context reasoning]
    guidance: "Budget 80k tok (cap). Use for discovery/grill when many trackers."

  opencode/qwen3.5-plus:
    context_window: 1000000
    max_output: 65536
    capabilities: [tools, reasoning, code, long-context]
    cost: low
    strengths: [worker, fast iteration, long-context]
    guidance: "Budget 80k tok (cap). Checklist-style despite 1M window."

  opencode/muse-spark-1.2:
    context_window: 1048576
    max_output: 131072
    capabilities: [tools, reasoning, structured-output, vision, long-context]
    cost: low
    strengths: [orchestrator, planning, long-context]
    guidance: "Budget 80k tok (cap 1M*0.30). Can ingest many files verbatim when as orchestrator."

  opencode/muse-spark-1.2-contributor-free:
    context_window: 1048576
    max_output: 131072
    capabilities: [tools, reasoning, structured-output, vision, long-context]
    cost: low
    strengths: [orchestrator, planning, long-context, contributor-free]
    guidance: "Budget 80k tok (cap). Same as muse-spark-1.2 — inherit gives planner/reviewer/architect full window."

  opencode/grok-4.5:
    context_window: 500000
    max_output: 500000
    capabilities: [tools, reasoning, long-context]
    cost: medium
    strengths: [general, long-context]
    guidance: "Budget 80k tok (cap). Large window; cap keeps cost similar to gpt-5."
